<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="hr.unipu.mobilne.notes.UredjivanjeActivity">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Naslov bilješke"
        android:id="@+id/naslov_biljeske"
        android:layout_marginBottom="20dp"
        />
    <EditText
        android:layout_width="match_parent"
        android:layout_below="@+id/naslov_biljeske"
        android:hint="Tekst biljeske ide ovdje"
        android:layout_height="wrap_content"
        android:minLines="8"
        android:scrollbars="vertical"
        android:lines="8"
        android:gravity="top|left"
        android:inputType="textMultiLine"
        android:id="@+id/tekst_biljeske" />

    <Button
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:text="Spremi izmjene"
        android:textSize="20sp"
        android:onClick="spremi_izmjene"
        android:layout_height="wrap_content" />

</RelativeLayout>
